#include <stdio.h>

int main(void) {
    int T,x,disc;
    scanf("%d",&T);
    
    for(int i=0;i<T;i++)
    {
        scanf("%d",&x);
        disc= 100-x;
        printf("%d\n",disc);
    }
	return 0;
}
